# Backend environment variables (local dev)
# Keep this in sync with frontend/.env (VITE_API_KEY)
API_KEY=kzi207-khoaktck-cncd2511

# Optional: lock CORS to local dev origins
# CORS_ORIGIN=http://localhost:3000,http://localhost:5173
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=diemdanh